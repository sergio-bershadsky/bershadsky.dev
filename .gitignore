node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.local/
attached_assets/

# Pre-publication article specs and story plans — never committed
.draft/

# Local secrets (TELEGRAM_BOT_TOKEN, OPENAI_API_KEY)
.env
